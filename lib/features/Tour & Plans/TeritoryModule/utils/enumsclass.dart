enum TerritoryLayer {
  area,
  doctor,
  beat,
}

enum VisitStatus {
  pending,
  visited,
  missed,
}

enum DoctorCategory {
  aPlus,
  a,
  bPlus,
  b,
  c,
}

enum BeatColor {
  blue,
  green,
  orange,
  purple,
  red,
  cyan,
}

enum TerritoryMapMode {
  view,
  createBeat,
  editBeat,
  viewBeat,
}